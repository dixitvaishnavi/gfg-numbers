class Solution
{
    public long sum_of_square_evenNumbers(long n)
    {
        // Code here
        long sum=0,num=2;
       for(int i=0;i<n;i++)
       {
           sum=sum+(long)Math.pow(num,2);
           num+=2;
       }
       return sum;
    }
}
